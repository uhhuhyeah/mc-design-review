- if params[:scope] && params[:scope] == 'all'
  = link_to 'See Recent Talks', talks_path, class: 'btn'
  = will_paginate @talks 
- else
  = link_to 'See All Talks', talks_path(scope: :all), class: 'btn'

